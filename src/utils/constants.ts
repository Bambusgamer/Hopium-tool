
/**
 * Url from which the token gets scraped
 */
export const baseUrl = 'https://h2.live';

/**
 * Url where all the required data is fetched from using the scraped token
 */
export const apiUrl = `https://fuelstations.h2-api.live/v1/fuelstation`;
